{
  "name": "openindexwiki",
  "version": "0.1.0",
  "private": true,
  "description": "OpenIndex Wiki: CLI, MCP server and agent skill for an index of knowledge for AI agents",
  "workspaces": [
    "shared",
    "cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
